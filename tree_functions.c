#include <stdio.h>
#include <stdlib.h>
#include "tree_def.h"
#include "tree_operations.h"

tree *tree_init()
{
    tree *root = malloc(sizeof(tree));
    root = NULL;

    return root;
}

tree *tree_insert(tree *root){
    
}
